import React, { ReactNode } from "react";

/**
 * MainGame 界面下方的剪辑时间线组件
 * @param children
 * @param props
 * @constructor
 */
export const EditorTimeline = ({
  children,
  ...props
}: {
  children?: ReactNode;
}) => {
  return (
    <>

    </>
  );
};
